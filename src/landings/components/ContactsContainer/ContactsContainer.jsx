import React from 'react';

import './ContactsContainer.less';

function ContactsContainer({ children }) {
  return <div className="ContactsContainer">{children}</div>;
}

export default ContactsContainer;
